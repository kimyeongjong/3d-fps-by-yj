Original prompt: 현대적인 도시 배경의 3d fps 게임을 만들어줘.

2026-03-11
- Vite + Three.js 기반 단일 캔버스 FPS 프로젝트를 새로 세팅했다.
- 시작 오버레이, HUD, 포인터 락, 이동/점프/사격, 적 웨이브, 점수/체력, 일시정지, 전체화면, 재시작, `window.render_game_to_text`, `window.advanceTime` 골격을 추가했다.
- 의존성 설치와 프로덕션 빌드는 통과했다.
- Playwright 기본 클라이언트로 첫 실행을 했고, 자동화 환경에서 포인터 락 예외가 발생해 방어 처리했다.
- 중앙 시야에서 전투가 바로 보이도록 적 스폰 위치와 일부 건물 배치를 조정했다.
- 실제 스크린샷에서 첫 적이 중앙 조형물에 가려 보여 조형물 위치를 옮겨 시야를 열었다.
- Playwright 기본 클라이언트 재실행으로 이동, 점프, 사격, 적 처치, 점수 반영, 스크린샷, 상태 JSON, 콘솔 무오류를 확인했다.
- 추가 Playwright 런타임 검증으로 `P` 일시정지/재개, `F` 전체화면, 장시간 진행 후 게임오버, `R` 재시작과 상태 초기화를 확인했다.
- 남은 이슈는 기능상 버그가 아니라 번들 크기 경고뿐이며, 현재 범위에서는 출시 차단 이슈가 아니다.
- 후속 수정으로 이동 기준을 `cameraYaw` 추정값 대신 실제 카메라 월드 방향의 수평 투영으로 바꿔, 시선 각도에 따라 WASD 축이 어긋나는 문제를 줄였다.

TODOs / Suggestions for next agent
- 번들 크기 경고를 줄이려면 Three.js 코드 스플리팅 또는 씬/에셋 분리를 검토할 수 있다.
- 다음 단계 확장 아이디어로는 사운드, 더 다양한 도시 소품, 추가 적 패턴, 조준 보정 UI가 있다.
